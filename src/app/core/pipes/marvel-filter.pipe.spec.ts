import { MarvelFilterPipe } from './marvel-filter.pipe';

describe('MarvelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MarvelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
